import 'package:flutter/material.dart';

class Page extends StatelessWidget {
  const Page({Key key, this.texto}) : super(key: key);
  final String texto;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
