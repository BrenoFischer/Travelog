import 'package:flutter/material.dart';
import 'package:travelog/classes/date.dart';

class Page extends StatelessWidget {
  const Page({Key key, this.text, this.date}) : super(key: key);
  final String text;
  final Date date;

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
